════════════════════════════════════════════════════════
  ⚠️  MYSQL DATABASE CLEAR - FINAL FIX
════════════════════════════════════════════════════════

THE REAL ISSUE:
MySQL database still has the old emails!
That's why "Clear & Retry" doesn't work!

════════════════════════════════════════════════════════
STEP 1: OPEN MYSQL WORKBENCH
════════════════════════════════════════════════════════

1. Open MySQL Workbench (from Start menu)
2. Double-click "Pharmac Local" to connect
3. Enter your MySQL root password if prompted

════════════════════════════════════════════════════════
STEP 2: OPEN AND RUN CLEAR SCRIPT
════════════════════════════════════════════════════════

1. In MySQL Workbench, click: File → Open SQL Script
2. Navigate to: database/CLEAR_ALL_USERS.sql
3. Select it and click Open
4. Click the lightning bolt ⚡ icon (or Ctrl+Shift+Enter)
5. It will execute the SQL commands

You should see:
  ✓ Query OK
  ✓ 0 rows in set

════════════════════════════════════════════════════════
STEP 3: VERIFY USERS TABLE IS EMPTY
════════════════════════════════════════════════════════

1. On the left side, find: pharmac_db
2. Right-click it → Refresh
3. Expand: Tables
4. Find: users table
5. Right-click users → Select Rows - Limit 1000
6. You should see: 0 rows returned (empty table!)

════════════════════════════════════════════════════════
STEP 4: CLEAR BROWSER STORAGE
════════════════════════════════════════════════════════

1. Go to: http://localhost:5173/signup
2. Press: F12
3. Click: Console tab
4. Paste: localStorage.clear(); sessionStorage.clear(); location.reload();
5. Press: Enter

════════════════════════════════════════════════════════
STEP 5: RESTART SERVER (Important!)
════════════════════════════════════════════════════════

In VS Code Terminal:

1. Press: Ctrl+C (stop server)
2. Wait 2 seconds
3. Run: npm run dev
4. Look for: "Using MySQL storage (mysql2)"

════════════════════════════════════════════════════════
STEP 6: SIGNUP WITH FRESH EMAIL
════════════════════════════════════════════════════════

Form should now be completely clean!

Fill:
  Name: testuser
  Email: newtest@example.com (MUST BE NEW!)
  Password: test123456

Click: Create account

Result: SUCCESS! ✅

════════════════════════════════════════════════════════
WHAT IF IT STILL SHOWS ERROR?
════════════════════════════════════════════════════════

1. Check browser Console (F12):
   - Paste: console.log(localStorage.getItem('pharma_users'));
   - If it shows anything other than "null", clear again

2. Check MySQL Workbench:
   - Right-click users table → Select Rows
   - Should show 0 rows
   - If it shows data, run CLEAR_ALL_USERS.sql again

3. Check terminal for "Using MySQL storage":
   - If you see "falling back to JSON", restart server
   - MySQL might not be running properly

4. Try "Fresh Start" button on signup:
   - Full page reload
   - Clears everything and starts fresh

════════════════════════════════════════════════════════
KEY POINTS
════════════════════════════════════════════════════════

🔴 MySQL database HAS the emails → need to clear in Workbench
🔴 Browser localStorage HAS the emails → F12 console clear
🔴 Form fields still have old data → click "Fresh Start"

🟢 After all 3 are clear → signup will work!

════════════════════════════════════════════════════════
DETAILED TROUBLESHOOTING
════════════════════════════════════════════════════════

Q: Still getting "email already registered" even after clearing?
A: Check all 3 places:
   1. MySQL users table (right-click → Select Rows) should show 0 rows
   2. Browser localStorage (F12 → console.log(localStorage)) should be empty
   3. Form fields should be blank (click "Fresh Start" button)

Q: Can't connect to MySQL in Workbench?
A: 1. Check Services (Windows Key + services.msc)
      2. Find MySQL80 → Status should be "Running"
      3. If stopped, right-click → Start
      4. Wait 3 seconds
      5. Try connecting again in Workbench

Q: Don't see CLEAR_ALL_USERS.sql file?
A: It should be in:
   c:\Users\ADITYA\Downloads\dream-weaver-main (1)\Pharmac\database\

Q: Getting permission denied error?
A: You might need admin rights for MySQL
   - Right-click MySQL Workbench
   - Select "Run as administrator"
   - Try again

════════════════════════════════════════════════════════
THE COMPLETE CLEAR ORDER
════════════════════════════════════════════════════════

This is the correct order to clear everything:

1. MySQL Workbench → Run CLEAR_ALL_USERS.sql
   (Clears database)

2. Browser F12 → localStorage.clear()
   (Clears browser storage)

3. Click "Fresh Start" button
   (Resets form)

4. Ctrl+C then npm run dev
   (Restart server)

5. Try signup again
   (Should work!)

════════════════════════════════════════════════════════

Remember: You need to clear MySQL database, NOT just localStorage!
That's the real issue!
